/**
 * UI-related constants for layout and positioning
 */

export const UI_CONSTANTS = {
  /**
   * Horizontal offset for element popover relative to element bounds
   */
  ELEMENT_POPOVER_OFFSET_X: 5,

  /**
   * Default arrow type for line elements
   */
  DEFAULT_END_ARROW: 'arrow' as const,
} as const
